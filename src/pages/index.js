import Container from "@mui/material/Container";
import { Button } from "@mui/material";

const Homepage = () => {
  return (
    <Container maxWidth="sm">
      <Button variant="contained" color="primary">Olá</Button>
    </Container>
  );
};

export default Homepage;